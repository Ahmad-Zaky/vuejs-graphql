version: '3'
services:
  client:
      build:
        context: .
        dockerfile: .docker/node/Dockerfile
      ports:
        - "8080:8080"
      volumes:
        - .:/usr/src/app/
        - /usr/src/app/my-app/node_modules
      mem_limit: 1024m
      mem_reservation: 512M